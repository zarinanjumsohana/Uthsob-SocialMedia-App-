<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Top row: icon + title + time -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Icon -->
            <TextView
                android:id="@+id/notif_icon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:text="📈"
                android:textSize="20sp"
                android:gravity="center"
                android:background="@drawable/bg_circle_icon"/>

            <!-- Title -->
            <TextView
                android:id="@+id/notif_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="নতুন বিড পাওয়া গেছে!"
                android:textColor="@color/dark_green"
                android:textSize="13sp"
                android:textStyle="bold"
                android:layout_marginStart="10dp"/>

            <!-- Time -->
            <TextView
                android:id="@+id/notif_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="10:41 AM"
                android:textColor="@color/gray"
                android:textSize="11sp"/>

        </LinearLayout>

        <!-- Message -->
        <TextView
            android:id="@+id/notif_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Rahat Chowdhury placed a bid of ৳140"
            android:textColor="@color/gray"
            android:textSize="12sp"
            android:layout_marginTop="10dp"/>

        <!-- Action buttons (only for bid notifications) -->
        <LinearLayout
            android:id="@+id/action_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="12dp">

            <!-- Sell Now button -->
            <Button
                android:id="@+id/btn_sell_now"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:text="বিক্রয় করুন (Sell Now)"
                android:textColor="@color/white"
                android:textSize="11sp"
                android:background="@drawable/btn_green_solid"
                android:layout_marginEnd="8dp"/>

            <!-- Details button -->
            <Button
                android:id="@+id/btn_details"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:text="বিস্তারিত (Details)"
                android:textColor="@color/dark_green"
                android:textSize="11sp"
                android:background="@drawable/bg_input_field"/>

        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>